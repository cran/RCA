#
# Makevars for RCA
#

PKGB_PATH=`echo 'library(igraph); cat(system.file("libs", .Platform$$r_arch, package="igraph", mustWork=TRUE))' | ${R_HOME}/bin/R --vanilla --slave`
PKG_LIBS="$(PKGB_PATH)/igraph.so"

