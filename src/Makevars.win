#
# Makevars for RCA
#

PKGB_PATH=`echo 'library(igraph); cat(system.file("libs", .Platform$(r_arch), package="igraph", mustWork=TRUE))' | rterm --vanilla --slave`

PKG_LIBS = -L$(PKGB_PATH) -ligraph


